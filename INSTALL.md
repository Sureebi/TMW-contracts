# Инсталация на TMW Vehicle Sale Contract

## 📦 Стъпки за инсталация

### 1. Изтегляне
```bash
git clone https://github.com/YOUR_USERNAME/TMW-contracts.git
```

### 2. Инсталация
1. Копирайте папката `TMW-contracts` в `resources` директорията на вашия сървър
2. Преименувайте папката на `tmw-contracts` (малки букви)

### 3. Конфигурация

#### fxmanifest.lua
Файлът вече е конфигуриран правилно.

#### server.lua
Променете `itemname` ако искате различен предмет:
```lua
local itemname = 'contract' -- Променете тук
```

### 4. Добавяне в server.cfg
```cfg
ensure tmw-contracts
```

### 5. Добавяне на предмета в базата данни

#### За QBCore:
Добавете в `qb-core/shared/items.lua`:
```lua
['contract'] = {
    ['name'] = 'contract',
    ['label'] = 'Договор за продажба',
    ['weight'] = 0,
    ['type'] = 'item',
    ['image'] = 'contract.png',
    ['unique'] = false,
    ['useable'] = true,
    ['shouldClose'] = true,
    ['combinable'] = nil,
    ['description'] = 'Договор за продажба на превозно средство'
},
```

### 6. Добавяне на изображение
Копирайте изображение `contract.png` в:
```
qb-inventory/html/images/contract.png
```

## 🔧 Опционални настройки

### Progressbar
Скриптът автоматично открива:
- `progressbar`
- `qb-progressbar`

Ако нямате нито един от тези ресурси, ще се използва вградения QBCore прогресбар.

### Vehiclekeys
Уверете се че имате:
- `qb-vehiclekeys`
- или друга система за ключове

## ✅ Тестване

### Команди за тестване:
```bash
# Тестване на UI
/testui

# Тестване на функционалност (admin)
/testcontract ABC123 50000
```

## 🐛 Troubleshooting

### Проблем: "Играчът не е намерен"
- Уверете се че двамата играчи са близо един до друг (< 3.5m)
- Проверете дали QBCore работи правилно

### Проблем: "Невалидна цена"
- Уверете се че въвеждате число
- Цената трябва да е по-голяма от 0

### Проблем: UI не се показва
- Проверете F8 конзолата за грешки
- Уверете се че всички файлове са качени правилно
- Рестартирайте ресурса: `restart tmw-contracts`

## 📞 Поддръжка

За въпроси и проблеми, моля отворете Issue в GitHub репозиторията.
